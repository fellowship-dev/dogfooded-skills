# Correctness Checklist: Fix closingIssuesReferences Repo Predicate

**Purpose**: Validate that the spec's requirements around predicate correctness, fail-closed
behavior, and dual-site consistency are complete, unambiguous, and objectively verifiable —
not that the implementation works (that's covered by tasks.md/tests).
**Created**: 2026-09-11
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `speckit.checklist` command based on feature context
and requirements.

## Completeness

- [x] CHK001 Does the spec define the exact real-world payload shape the predicate must operate against? [Spec §Key Entities]
- [x] CHK002 Does the spec state which two call sites must carry the corrected predicate? [Spec §Input, FR-002]
- [x] CHK003 Does the spec define what happens when the target repo has no matching closing reference (negative case), not just the positive match? [Spec §User Story 1, Acceptance #2]
- [x] CHK004 Does the spec address both consumers' distinct outcomes on match failure — postcondition rejection (Step 8) vs. gate falling through to PR creation (Step 0)? [Spec §User Story 2, Acceptance #2]

## Clarity

- [x] CHK005 Is "byte-identical" in FR-002 unambiguous about what substring must match (the predicate clause vs. the whole file)? [Spec §FR-002]
- [x] CHK006 Is the replacement expression for the repo match fully specified (exact field path and join logic), leaving no implementation choice to interpretation? [Spec §FR-001]

## Consistency

- [x] CHK007 Do User Story 1 and User Story 2's acceptance criteria use the same definition of "matching repo" (no drift between the two sites' expected behavior)? [Spec §User Story 1, §User Story 2]
- [x] CHK008 Does the Key Entities shape agree with the field names referenced in FR-001 (`repository.owner.login`, `repository.name`)? [Spec §Key Entities, FR-001]

## Measurability

- [x] CHK009 Is SC-001 objectively checkable via a single deterministic command and exit code, without subjective judgment? [Spec §SC-001]
- [x] CHK010 Is SC-002's "must-fail-before" criterion falsifiable — does it specify exactly what gets reverted and what must remain fixed while reverted? [Spec §SC-002]
- [x] CHK011 Is SC-003 scoped to a specific, non-ambiguous search path and pattern? [Spec §SC-003]

## Coverage

- [x] CHK012 Are both P1 user stories (Step 8 postcondition, Step 0 resume gate) independently testable per the spec, without one masking a defect in the other? [Spec §User Story 1, §User Story 2]
- [x] CHK013 Does the spec distinguish in-scope call sites from other `nameWithOwner` usages elsewhere in the codebase that must NOT be touched? [Spec §Assumptions]

## Edge Cases

- [x] CHK014 Does the spec define behavior when the `repository` key is absent from a closing-reference entry? [Spec §Edge Cases]
- [x] CHK015 Does the spec define behavior when `closingIssuesReferences` is an empty array? [Spec §Edge Cases]
- [x] CHK016 Does the spec rule out a jq execution error as an acceptable outcome for either edge case (i.e., require fail-closed, not fail-crash)? [Spec §FR-003]
- [x] CHK017 Does the spec explicitly place `any(...)` cardinality semantics and the unrelated branch-misnaming defect out of scope, preventing scope creep during implementation? [Spec §Assumptions]

## Notes

- All 17 items trace to explicit spec.md sections — no gaps found; this checklist confirms the
  spec was sufficiently precise going into implementation, it does not re-validate the code.
- Complements `checklists/requirements.md` (general requirement-writing quality, done in the
  specify phase) with a domain-specific pass on predicate-correctness precision.
